sync:
  defaults:
    ignore:
      vcs: true
      paths:
        - "node_modules"
        - ".git"
        - ".DS_Store"
        - "tmp"
        - "logs"
        - "mutagen.yml.lock"
        - "data"
  zerobyte:
    alpha: "."
    beta: "nicolas@192.168.2.42:/home/nicolas/zerobyte"
    mode: "one-way-replica"
    flushOnCreate: true
    ignore:
      paths:
        - "node_modules"
